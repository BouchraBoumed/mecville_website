{
  "name": "mecville",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,react -c green,yellow \"npm run dev --prefix back-end\" \"npm run dev --prefix front-end\"",
    "dev:backend": "npm run dev --prefix back-end",
    "dev:frontend": "npm run dev --prefix front-end",
        "build": "npm run build --prefix front-end",
    "stop": "echo \"No stop needed — use Ctrl+C to stop dev servers\""
  },
  "devDependencies": {
        "concurrently": "^8.2.2"
  }
}
